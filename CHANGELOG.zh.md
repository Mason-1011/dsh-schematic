# 变更日志

开发里程碑,新在前。这些版本号记录的是仓库自身的进度;npm 自 v0.2.27 起正式发版。下一步见 README 的[路线图](README.zh.md#路线图)。English version: [CHANGELOG.md](CHANGELOG.md).

- **v0.2.27** — 尺寸上限解除 + 铺满修复:拖角手柄不再有固定最大宽高——视口本身是唯一上限(拖拽钳制在窗口边缘,保存的尺寸在下次打开时按当前窗口重新钳入);同时修复星系布局在极端纵横比下的铺满缺陷——力松弛阶段只有局部斥力与向心拉力,长轴两端各留 ~15% 空带(细高面板尤为明显),现于求解后把星点包围盒仿射铺满回边距框(每轴只拉伸、间距只增不减,数学上不产生新重叠),32～1560 全档纵横比零重叠且铺满到上限;追赶修复:停靠位放不下保存的宽度时不再整个隐藏面板——按当前停靠条宽度收缩显示(纵横比跟随收缩后的尺寸重排,保存的尺寸不丢,窗口腾出空间自动长回),自由摆放同样按视口收缩,只有连最小宽度都放不下的极端窄窗才隐藏;设置导航图标经用户五轮意见定稿为 Steam 式斜向三圆链(三圆等大、仅链尾最右端实心、其余两圆空心描边;三个圆沿 Steam 标志的 ~45° 对角线布置——镜像后两个圆环在高处、实心圆落在连杆末端的低位,两段圆头连杆共一个缓膝、绝不折成 90° 直角也绝不排成一条直线;即最小拓扑图:流经三节点、终结于实心点);底色滑杆标题「星座面板底色」更名「星图面板底色」;**首个 npm 发布**:包声明 dsh.bundle(自注册层)并携带构建产物——宿主半边打包为 dist/index.js(纯 JS 入口,dist/web/index.html 随附),一条命令安装 `dsh plugin --profile web add dsh-schematic`
- **v0.2.26** — 圆点悬停名片:鼠标停在星图的任一圆点上,浮现该插件的名片——与星色一致的色点、短名、包 id、一句话说明、连线数;中文说明复用查看页的共享翻译缓存,缓存未命中时当场按需补译一条(写入查看页同款的 sch.zhmap 存储,两个入口互相暖同一份缓存);卡片跟随光标、贴近窗口边缘自动翻转,拖动/缩放面板时收起,面板原生提示不与之叠显
- **v0.2.25** — 底色入设置 + 原子键图标:底色模糊与浓度同一刻度——backdrop-filter 的模糊半径随 --bgA 走,0% 时面板彻底无感(此前固定 3px 模糊让"透明"面板仍在磨砂背后内容);设置-插件拓扑新增底色滑杆,与滚轮双向实时同步(同一自定义事件,滑杆开着时滚轮会带动滑杆);设置导航里本分区的默认齿轮换成自绘图标(齿轮是宿主按分区 id 写死的,插件侧以观察者在弹层挂载时替换,不触碰宿主代码;图标本体在 v0.2.27 又经五轮意见重设计)
- **v0.2.24** — 星野质感:未归类的包不再是一片中性灰——每个包按模块哈希从六档柔和星色取色,圆点亮度随连线数分层(枢纽亮、边缘暗);包点亮的瞬间多一圈一次性扩散涟漪;面板垫上深空底色,透明度用鼠标滚轮随调(0–100%,按浏览器记忆,调回 0 即恢复全透明);面板配色全部改走 `light-dark()` 跟随页面 color-scheme——此前暗色变体挂在操作系统偏好上,应用主题与系统主题不一致时会错配
- **v0.2.23** — 自由摆放 + 星系布局:拖动面板本体可移到屏幕任意位置(钳制在视口内,按浏览器持久化;拖回输入卡旁自动重新吸合成跟随模式);星图布局弃用均匀圆环排布,改为确定性的力松弛星系——先按连线数排序沿黄金角螺旋播种,再用排斥/弹簧/向心力散开成有疏密的团块,枢纽画成更大的环(纯函数于图+纵横比,重渲染不重洗牌);调宽拖到窗口边缘时停在边缘而不是隐藏面板,无按键的游走指针事件不再污染拖拽
- **v0.2.22** — 缩影加固 + 可调大小:传输改为 ~1.2 秒轮询新增的 /schematic/mini.json(按序号游标差分行 + 全量实时状态 + 近窗服务读写)——v0.2.21 每个标签页长持的那条 SSE 连接会吃掉浏览器同源 6 条 HTTP/1.1 连接预算中的一条,多开几个标签页后 SPA 自己的启动 RPC 被饿死、刷新卡在加载页;面板同时抬到输入卡吸顶层的上方(z 12 > 7),圆点不再被卡片的磨砂表面吞掉或抢走双击;空闲圆点改为空心环,点亮时填充并绽放双层光晕;右下角手柄可自由调整宽高(各自独立限幅,按浏览器持久化;星图的环按新纵横比重新铺排,不产生黑边)
- **v0.2.21** — 输入框旁的实时缩影:领域"全部展开"时的药丸放射图缩为输入卡右侧的一枚星图小窗——挂在卡片右缘外 12px、与整卡垂直居中(视口定位,每 0.8 秒重新贴附卡片矩形,跟随草稿长高;窗口放不下时自动隐藏);一包一点、按连线数从中心向外排布(圆点沿用查看页的家族配色),连线极淡;随当前会话的真实运行点亮(工具执行期强高亮呼吸,模型流式、服务读写、宿主动作微亮后按 TTL 衰减),会话取自 SPA 自己持久化的当前选择;双击小窗即在新标签页打开查看页的领域视图并直接处于全部展开状态(查看页同版本新增 `?expand=all` 深链)
- **v0.2.20** — 后台任务可观测:jobs 注册表是活状态——它只经自身回调(onJobsChanged/onJobDone)报告变化,不写任何会话日志,后台 bash 此前只有发起它的那一行工具调用可见;采集器现在以纯监听者身份订阅(不加拓扑边、不写入、不包装),把启动/落定行落入时间线——标签、终态、运行时长——归因到 jobs 提供方并点亮其药丸;页面内的渲染插件(client-ui-jobs)按设计保持不亮:它活在浏览器进程,宿主侧没有任何可观察的足迹
- **v0.2.19** — 连线方向 = 服务流向:每条边按"提供方 → 注入方"绘制,箭头落在注入方上——服务从提供者流向使用者(旧箭头沿用"依赖指向"画法,方向恰好相反,且与 v0.2.14 起文档承诺的指向矛盾);图例、悬停标题与方向说明同步改写
- **v0.2.18** — 分组一键展开:总览里每张分组卡(核心脊柱、家族、聚类)悬停即现右上角 ⊕,一键把该组打散成成员药丸并整体重排网状布局——成员按自身连线数定秩,原本藏在卡内的组内连线也随之可见;分组详情面板提供"在总览中展开/收回为一张卡",成员详情面板可单点收回所在组;页脚 fit 旁新增全部展开/全部收起按钮(全部展开为并集:已打散的组保持打散,只在所有可展开组都已打散时才显示"全部收起")
- **v0.2.17** — 领域铺满:放射网状布局同样在任意窗口比例下铺满画布——药丸沿椭圆逐行排布,轴比按"适配缩放的宽/高两项交叉点"二分求解(画布能不裁切显示的最宽网状),窗口缩放时自动重排
- **v0.2.16** — 适配铺满画布 + 未分组代发:旅程页"适配"改为按可用宽度求解铺满——药丸在求解出的宽度下自动换行,任意窗口比例都不再留信箱式大边;"对话中询问"落在未分组新会话,并由插件经 RPC 网关代发第一问——发送本身就解锁输入框(无工作区空白会话的输入框按 harness 设计锁定),发送失败则退回预填草稿
- **v0.2.15** — 核心脊柱卡:agent-loop、system-prompt、tools 只依赖通用 ctx 键,不构成任何能力接缝——领域总览把它们归为一张可打开的"core"卡(双击或详情面板打开按钮进入作用域视图),位于网状中心、连线最多;杂项"other"家族解散,剩下的零散包以单药丸散布
- **v0.2.14** — 领域总览网状布局:领域总览弃用列式排布,改为放射网状——单元按单元级边数排序、从中心向外连续填充,越靠中间连线越多(不设显式分层);带碰撞检查的环形逐行摆放保证圆盘紧凑且卡片零重叠,连线改为锚定卡片边框的直线(箭头指向消费方),布局完全确定;聚类/家族内部的作用域视图仍保持语义列
- **v0.2.13** — 服务访问可观测:纯接线的包(只提供/注入服务、不监听任何广播)第一次有了实时信号——一个 internal/get waterfall 监听(框架自带的"服务正被读取"扩展点)把每次 ctx.服务键 读取计为(读方包, 键),行为中立(只计数透传,绝不包装返回值);时间线出现可开关的"服务访问"行,访问方与提供方双侧点亮,详情面板列出每个包实际访问过的键及次数;一个回合能追到 agent-loop → ctx.llm、tool-bash → ctx.shell ×3 + sandbox/shellEnv/subprocess
- **v0.2.12** — 广播接收方:运行中的事件总线保留每条监听记录(ctx.events._hooks,经注册 fiber 归因到挂载包),查看页由此能回答"这条广播谁在接收"——活动条标出 session/event 的监听插件,时间线每个非操作行悬停即见接收方名单,每个包的详情面板列出它监听的事件;接收方随后的反应仍以自己的归属行出现在时间线里
- **v0.2.11** — 旅程自适应缩放:旅程页自动缩放阶段卡片,任意窗口尺寸下整条消息路径零滚动条一屏看全;+/−/适配按钮可放大到合适阅读的倍数(拖拽平移与滚动条接管),再点恢复总览
- **v0.2.10** — 家族卡可打开 + 流式高亮修复:领域视图的家族卡(同前缀包聚合)与能力聚类一致——双击或"打开分组"进入同款作用域视图;模型流式输出结束后的强高亮此前永不衰减(llm 家族卡长亮),现在随消息完成降级为呼吸光并按 TTL 熄灭
- **v0.2.9** — 全动作可观测:RPC 网关观察者以行为中立的中转包装 apiProxy 全部变更方法,活注册表事件(tools/change、system-prompt/change 等)汇入同一条数据流——归档会话、重命名工作区、修改设置这类从不写会话日志的界面动作,同样点亮所属插件并以带耗时的操作行落入时间线;对宿主进程仍是纯旁观
- **v0.2.8** — 运行时活动:SSE 数据流(`/schematic/events`)在进程内旁观会话事件洪流,把每个回合、模型回复、工具调用归因到所属插件;查看页实时点亮当前工作流经的插件(工具执行/模型流式输出期间强高亮,之后呼吸光衰减),附带可折叠的活动时间线条与会话选择器,默认跟随 SPA 当前聊天会话(纯旁观,绝不向会话日志写入任何内容)
- **v0.2.5** — 可读拓扑:三个标签页——旅程(一条消息在运行时流经的八个阶段卡片及各阶段交换的 ctx 键)、领域(无能力接缝的同前缀包归为家族卡;每条边都是带悬停卡的箭头,标明消费方、提供方与注入的 ctx 键)、表格;每 5 秒刷新并弹变化提示,新插件脉冲高亮,`?tab=` 深链
- **v0.2** — 实时拓扑:作为插件挂载,合并 Cordis 运行时 × loader 三路数据,在 `/schematic` 提供查看页;一键中英整页切换(描述由进程内 LLM 批量翻译,标识符保持英文),dsh 设置分区按钮在新标签页打开,插件解读新建未分组对话
- **v0.1** — 静态拓扑:扫描 harness 仓库(`tools/scan.mjs`),从 `inject` 声明渲染插件依赖图
- **v0.0.x** — 占位名称、定位文档
